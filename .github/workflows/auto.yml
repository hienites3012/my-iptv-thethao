name: Auto Crawl Football IPTV

on:
  schedule:
    - cron: '*/30 * * * *' # Tự động chạy cào lại mỗi 30 phút
  workflow_dispatch: # Cho phép bấm nút chạy tay trên web GitHub

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install curl_cffi requests

      - name: Run script
        run: python update_iptv.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add playlist.m3u
          git commit -m "Auto update football matches" || exit 0
          git push